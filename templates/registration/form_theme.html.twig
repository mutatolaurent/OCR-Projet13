{% block form_errors %}
    {%- if errors|length > 0 -%}
            {%- for error in errors -%}
                <span class="erreur-item">&#10060; {{ error.message }}</span>
            {%- endfor -%}
    {%- endif -%}
{% endblock %}